---
// Admin login has been consolidated into the general /login page
// This page redirects to the unified login system with admin redirect parameter
return Astro.redirect('/login?redirect=/admin');
---
